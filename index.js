// n8n loads nodes/credentials via the "n8n" attribute in package.json,
// not via this file. It exists only so `main` resolves and tooling that
// requires the package doesn't error.
module.exports = {};
